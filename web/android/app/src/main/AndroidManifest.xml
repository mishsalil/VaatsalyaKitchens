<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Our messaging service extends Capacitor's and calls super, so the
             plugin still gets every message. Firebase delivers to exactly one
             service, so the plugin's own registration is removed here and ours
             takes the intent filter; leaving both would be a coin toss over
             which one Android picks. -->
        <service
            android:name="com.capacitorjs.plugins.pushnotifications.MessagingService"
            tools:node="remove" />
        <service
            android:name=".VkMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- Rings for an urgent order. shortService needs no extra permission
             and Android stops it after a few minutes on its own, which suits an
             alert that already gives up after a minute. -->
        <service
            android:name=".OrderAlarmService"
            android:exported="false"
            android:foregroundServiceType="shortService" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- "Use my current location" on the address picker. The page calls
         navigator.geolocation; Capacitor's BridgeWebChromeClient answers the
         WebView's prompt by requesting these two at runtime. Android refuses a
         runtime request for a permission that is not declared here, and refuses
         it SILENTLY — no dialog, an immediate denial — which is why the button
         appeared to do nothing at all in the app while working on the web. -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- Notifications. POST_NOTIFICATIONS is a runtime permission from Android
         13; the Capacitor push plugin asks for it at registration time. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Lets the urgent channel's setBypassDnd(true) actually take effect. This
         one is NOT granted by a runtime dialog: the user must switch it on in
         system settings, which is a one-time setup step on a counter device.
         Declared here so that settings screen lists this app at all. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- The urgent-order alarm. FOREGROUND_SERVICE lets it keep ringing while
         the app is in the background; WAKE_LOCK lights the screen so the alert
         is seen as well as heard. USE_FULL_SCREEN_INTENT is requested so the
         alert can take over the screen — on Android 14+ an app outside the
         alarm/calling categories may not be granted it, in which case the
         notification degrades to a heads-up banner and the tone still rings. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SHORT_SERVICE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!-- Printing to the counter's Bluetooth printer. Only BONDED devices are
         used — the printer is paired once in Android settings — so BLUETOOTH_SCAN
         is deliberately not requested: scanning is the permission that asks for
         nearby-devices access, and we never scan.
         BLUETOOTH_CONNECT is runtime from Android 12; the two legacy permissions
         cover older phones and are capped so they are not requested twice. -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
</manifest>
