ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
    /* The push plugin declares firebase-messaging as 'implementation', so its
       classes are hidden from this module. VkMessagingService subclasses the
       plugin's service, so the app needs it on its own compile classpath.
       Set here rather than in app/build.gradle because the plugin reads this
       same property — one version, no drift. */
    firebaseMessagingVersion = '25.0.1'
}